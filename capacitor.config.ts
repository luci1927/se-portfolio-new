import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tharindu.portfolio',
  appName: 'tharindu-portfolio',
  webDir: 'dist'
};

export default config;
